import '../../config.dart';

class SplashProvider extends ChangeNotifier {
  // Placeholder provider for splash screen
  // Currently not managing any state, kept for future use if needed
  initState(context, splashScreenState) {
    // No operation needed
  }
}
